name: Test

on:
  pull_request:
    branches:
      - master
    paths:
      - charts/**/*
      - script/**/*
      - .github/**/*
  push:
    branches:
      - master
    paths:
      - charts/**/*
      - script/**/*
      - .github/**/*

env:
  KUBEVAL_VERSION: "0.16.1"
  HELMDOCS_VERSION: "1.5.0"

jobs:
  lint:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Setup chart testing
        uses: helm/chart-testing-action@v2.3.0
      - name: Lint
        run: ct lint --config .github/config/ct-lint.yaml --check-version-increment=false
      - name: Lint docs
        run: .github/script/helm-docs.sh

  kubeval:
    runs-on: ubuntu-20.04
    needs:
      - lint
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Run kubeval
        run: .github/script/kubeval.sh

  install:
    runs-on: ubuntu-20.04
    needs:
      - lint
      - kubeval
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Setup chart testing
        uses: helm/chart-testing-action@v2.3.0
      - name: Check changes
        id: changes
        run: |
          changed=$(ct list-changed)
          if [[ -n "$changed" ]]; then
            echo "::set-output name=changed::true"
          fi
      - name: Create kind cluster
        if: steps.changes.outputs.changed == 'true'
        uses: helm/kind-action@v1.3.0
      - name: Install
        if: steps.changes.outputs.changed == 'true'
        run: ct install --config .github/config/ct-install.yaml
